# Shadow-Key
Shadow Key is a lightweight C++ password manager that protects user credentials using XOR encryption. It supports multiple isolated user profiles, 4-digit PIN authentication, and complete local file-based vault management (CRUD operations).
